import React from 'react';
import ReactDOM from 'react-dom/client';  // ✅ this must be correct
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
